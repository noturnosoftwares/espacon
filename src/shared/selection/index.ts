/**
 * Canal de seleção compartilhado (ADR — "Tela de listagem como consulta
 * reutilizável: modo gestão × modo seleção"). Tipos + store de handoff + o
 * composable que liga uma listagem ao modo seleção.
 */
export * from './selection-types'
export * from './selection-store'
export * from './use-selection-mode'
