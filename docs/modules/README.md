# Módulos — EspaçoN

Documentação por módulo de negócio (`src/modules/{module}`). Cada módulo segue a
estrutura corporativa `domain / data / presentation` (ver `docs/architecture` e
`../template/docs/architecture`). A especificação detalhada de cada funcionalidade
vive em `docs/specifications`; aqui fica a **visão do módulo** e o que é específico
dele.

## Índice

- [Usuários, Perfis e Permissões](users.md) — `src/modules/users`
